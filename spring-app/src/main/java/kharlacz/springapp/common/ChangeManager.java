package kharlacz.springapp.common;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class ChangeManager {
    
//    private final Map<Observable, List<Observer>> observableMapping = new HashMap<>();
//    
//    public void register(Observable observable, Observer observer) {
//        final var observers = observableMapping.get(observable);
//        observers.add(observer);
//    }
//    
//    public void unregister(Observable observable, Observer observer) {
//        final var observers = observableMapping.get(observable);
//        observers.remove(observer);
//    }
//    
//    public void notify(Observable observable) {
//        final var observers = observableMapping.get(observable);
//        observers.forEach(o -> o.update(observable));
//    }
}
